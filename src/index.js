import init from 'Touch/index.js';

const imageCheck = {
    init() {
        init();
    },
    bind(dom) {
        // bind(dom);
    },
};

imageCheck.init();
// imageCheck.bind('.img');

module.exports = imageCheck;
export default init;
